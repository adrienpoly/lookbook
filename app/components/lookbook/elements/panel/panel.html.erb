<%= render component_tag(x: {data: "panel"}, data: {as: @as}) do %>
  <div class="panel-content">
    <%= content %>
  </div>

  <% if actions? %>
    <div class="panel-actions">
      <% actions.each do |action| %>
        <%= action %>
      <% end %>
    </div>
  <% end %>
<% end %>
